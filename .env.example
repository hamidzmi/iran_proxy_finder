# Iran Proxy Finder - .env example
# Copy this file to `.env` and adjust values as needed.
# If you skip creating `.env`, defaults are applied automatically by docker-compose and the app.

# HOST_PORT: Host port mapped to the container's 5000 (web UI)
# Example: 5000 (default) or 5001 if 5000 is busy
HOST_PORT=5000

# TARGET_URLS: Comma-separated list of target URLs to test proxies against.
# If set, it overrides TARGET_URL and the built-in defaults.
# Example: https://api.ipify.org?format=json,https://httpbin.org/get,https://icanhazip.com
TARGET_URLS=

# TARGET_URL: Single target URL to test if TARGET_URLS is not provided.
# In CLI mode, the app falls back to general endpoints when not set.
# In tester, when no explicit target is passed, the default is https://ib.tejaratbank.ir/
TARGET_URL=https://api.ipify.org?format=json

# MAX_PROXIES: Limit how many scraped proxies are tested (positive integer).
# Example: 200 means only the first 200 proxies are tested.
MAX_PROXIES=200

# MAX_PER_TARGET: Limit how many proxies are tested per target (positive integer).
# Prevents long runs per target; useful when using multiple targets.
MAX_PER_TARGET=100

# VERIFY_IR: Enforce that the proxy exit IP geolocates to Iran.
# Set to 1 to enable (uses ipapi.co), 0 to disable.
VERIFY_IR=1

# OUTPUT_FILE: Where to write the results JSON.
# Leave empty for default app/working_proxies.json
OUTPUT_FILE=

# AUTOSTART: Automatically start a scan when the web server boots.
# Set to 1 to enable, 0 to disable.
AUTOSTART=0
